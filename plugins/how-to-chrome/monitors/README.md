# monitors/

Placeholder. Reserved for background monitor definitions belonging to the
`how-to-chrome` plugin, listed in a `monitors/monitors.json` file at this
plugin's own source root (`plugins/how-to-chrome/`). A monitor watches logs,
files, or external state in the background and notifies Claude when events
arrive, started automatically while the plugin is active.

Not used yet, no `monitors.json` in this folder. See the
[plugin structure reference](https://code.claude.com/docs/en/plugins-reference#monitors).
