# agents/

Placeholder. Reserved for custom agent definitions (`agents/<name>.md`)
belonging to the `grill-me` plugin, loaded from this plugin's own source
root (`plugins/grill-me/`).

Not used yet, no agents defined. See
[Subagents](https://code.claude.com/docs/en/sub-agents) and the
[plugin structure reference](https://code.claude.com/docs/en/plugins-reference).
