{
  "manifest_version": 3,
  "name": "Claude Tab Grouper",
  "version": "1.0.0",
  "description": "Groups the tabs Claude opens (via CDP) into a dedicated group, so they do not get mixed with yours.",
  "permissions": ["tabs", "tabGroups"],
  "background": { "service_worker": "bg.js" }
}
