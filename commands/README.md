# commands/

Placeholder. Legacy way to define skills as flat Markdown files at the
plugin root. New skills in this repo should use `skills/<name>/SKILL.md`
instead (see [`template/`](../template)); this folder is kept as a reserved
spot in case a plain slash-command style skill is ever needed.

Not used yet. See the
[plugin structure reference](https://code.claude.com/docs/en/plugins-reference).
